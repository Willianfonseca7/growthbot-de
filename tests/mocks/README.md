# Test Mocks

Centralize reusable test doubles here when a mock needs to be shared across suites.
Prefer focused mocks that express behavior over large object snapshots or implementation-heavy stubs.
